import { Routes } from '@angular/router';
import { ResizableDraggableComponent } from './resizable-draggable.component';

export const DRAG_RESIZE_ROUTES: Routes = [
    {
      path: 'resize-drag',
      component: ResizableDraggableComponent
    }
];
